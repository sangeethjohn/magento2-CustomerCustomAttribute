# Magento2-CustomerCustomAttribute

A Magento 2 module that add custom Cutomer Address Attribute.

Features

Adds Customer Address Attribute address_type 
Plugin added to display the attribute in the frontend
